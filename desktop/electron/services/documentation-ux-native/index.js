// Stable entry for Documentation UX.
// The implementation uses the phone's own SystemUI screenshot path and avoids
// loading image-processing native modules in Electron main.

export { default } from '../documentation-ux-phone/index.js'
